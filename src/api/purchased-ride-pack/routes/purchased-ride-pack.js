'use strict';

/**
 * purchased-ride-pack router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::purchased-ride-pack.purchased-ride-pack');
