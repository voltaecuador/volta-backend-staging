'use strict';

/**
 * purchased-ride-pack controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::purchased-ride-pack.purchased-ride-pack');
