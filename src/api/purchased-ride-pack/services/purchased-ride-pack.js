'use strict';

/**
 * purchased-ride-pack service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::purchased-ride-pack.purchased-ride-pack');
