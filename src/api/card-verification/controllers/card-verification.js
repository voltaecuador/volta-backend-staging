'use strict';

/**
 * card-verification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::card-verification.card-verification');
